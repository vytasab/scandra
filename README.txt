===============================================================================
MAVEN ANDROID PLUGIN VERSION 2 - SAMPLES
===============================================================================

The project home page is at http://code.google.com/p/maven-android-plugin

Documentation can be found there as well as in README.txt files in the folders
of the different examples.

-------------------------------------------------------------------------------
Specifically, for a stable version of the samples which work with a released
version of Maven Android Plugin, please see:

http://code.google.com/p/maven-android-plugin/wiki/Samples

-------------------------------------------------------------------------------
B906-2/vsh
    mvn clean install android:deploy
    mvn install android:deploy
    mvn android:deploy

------------------------------------------------------------------------------
 /*
 startActivity(new Intent(this, classOf[TemplateAttrib]).
 putExtra("ned", intent.getStringExtra("ned")).
 putExtra("attrib", arg._2).
 putExtra("groupId", templateGroupId).
 putExtra("matterCurrent", getIntent.getStringExtra("matter")).
 putExtra("thingStringCurrent", getIntent.getStringExtra("thingStringCurrent")).
 putExtra("thingString", getIntent.getStringExtra("thingString")))
 */
